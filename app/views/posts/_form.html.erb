<% if @post.new_record? %>
  <% form_url = posts_path %>
<% else %>
  <% form_url = post_path(@post) %>
<% end %>

<% form_for @post, :url => form_url  do |f| -%>
  <%= f.error_messages %>
  <div class="form-group">
    <%= f.label :title %>
    <%= f.text_field :title, :class => "form-control" %>
  </div>
  <div class="form-group">
    <%= f.label :body %>
    <%= f.tinymce :body, :class => "form-control" %>
  </div>

  <% unless @post.new_record? %>
    <div class="right">
      <%= link_to t('app.forms.delete'), post_path(@post), :class => "btn btn-danger", :confirm => "Are you sure?", :method => :delete %>
    </div>
  <% end %>

  <%= f.submit t('app.forms.submit'), :class => "btn btn-primary" %>
  <%= link_to t('app.forms.cancel'), :back, :class => "btn btn-link" %>
<% end -%>
